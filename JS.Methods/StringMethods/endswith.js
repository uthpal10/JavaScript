let j = "javascript";

// Returns true if the sequence of elements of searchString converted to a String is the same as the corresponding 
// elements of this object (converted to a String) starting at endPosition – length(this). Otherwise returns false.

let r = j.endsWith("script");
console.log(r);

let r1 = j.endsWith("Script");
console.log(r1);