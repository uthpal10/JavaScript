let s = "javascript";

// Returns the substring at the specified location within a String object.

// start — The zero-based index number indicating the beginning of the substring.

// end - Zero-based index number indicating the end of the substring. The substring includes the characters up to, 
// but not including, the character indicated by end. If end is omitted, the characters from start through the end
//  of the original string are returned.

let r = s.substring(4);
console.log(r);

let r1 = s.substring(4, 8);
console.log(r1);